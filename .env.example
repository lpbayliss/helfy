# Application Configuration
APP_NAME=helfy
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=helfy
DB_PASSWORD=password
DB_NAME=helfy_app
DB_SSL=false

# Development Notes:
# 1. Copy this file to .env for local development
# 2. Update DB_PASSWORD with a secure password for production
# 3. Set DB_HOST=postgres when running with Docker Compose